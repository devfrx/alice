---
name: agent-rework-plan
description: Multi-phase rework making the agentic path the only path + professional agent foundation
metadata: 
  node_type: memory
  type: project
  originSessionId: cd13dddb-cf89-4961-8cfe-b0ecefa49a5b
---

In corso (avviato 2026-06-07): rework per rendere la via agentica l'**unico** path e renderla professionale. Piano approvato in `C:\Users\Jays\.claude\plans\vorrei-lasciare-esclusivamente-la-tidy-storm.md`.

Sequenza: **Fase 0** rip-out (via toggle Chat/Agente, `agent.enabled`, `structured_mode`/`AgentTurnExecutor`, `voice_mode_bypass`; `DirectTurnExecutor` unico motore). **Fasi 1–3 fondazione behavior-preserving**: (1) engine transport-agnostic estratto da `_tool_loop.py` + `InteractionChannel` unico in ingresso (simmetrico a `WSEventSink`); (2) pipeline di middleware tool + `PermissionService` centrale con confinamento scope by-construction (capability tags su `ToolDefinition`) + spostare config conferme fuori da `pc_automation`; (3) stream eventi di turno canonico + run UI + re-inject piano. **Fasi 4–6 feature**: `ask_user` (tool `user_interaction`), piano come modulo persistito (`PlanService`), workspace scope mutabile solo in idle + plugin/modulo Terminale scoped.

Decisioni chiave dell'utente: rip-out totale (anche voice lite), fondazione completa prima delle feature, stream eventi canonico completo, domande inline in chat, terminale come plugin nativo (non MCP), scope per-conversazione settabile solo durante l'idle. Seam comandi `/` predisposto (solo contratto `CommandDefinition`/`get_commands()` in Fase 4), implementazione futura. Metodologia: fondazione (Fasi 1-3) = **manuale sequenziale** (codice accoppiato; subagent paralleli solo sui domini disgiunti o sulle feature 4-6, con review Opus a fine fase).

## STATO (aggiornato 2026-06-07)
- Branch di lavoro: **`agent-rework`** (off `main`).
- **Fase 0 COMPLETATA e committata** (`4bd3585`): verificata nel venv vero — 38 test backend verdi (incl. `test_app`), frontend typecheck pulito, zero nuovi errori ruff. Nuovo `backend/services/turn/_reflection.py` (slim, self-contained). Voice tool-trimming DIFFERITO (task #8: serve un segnale voce dal renderer; il seam `agent.voice.max_tools` è già in config).
- **Ambiente predisposto**: `.venv` a root con **Python 3.13.12 gestito da uv** (NON esisteva; c'era solo Python 3.14 globale) + backend `[dev,memory]` + `sqlite-vec`; frontend `npm install` fatto (vitest ora presente). Per eseguire: `& .\.venv\Scripts\python.exe -m pytest ...` da `backend/`.
- **Fase 1 (A+B) COMPLETA, committata** — 4 commit su `agent-rework`: `cca45fd` B-core (channel.py + 15 test), `6f11917` B-migrate (pump unico reader: ws.py idle→`next_user_message`, `run_tool_loop(channel,sink)`, rimossi `_ws_cancel_reader`/`_send_json`/`_spawn_cancel_reader`, `_request_confirmation`/`_execute_client_tool` su `channel.request`), `45b65d0` A (engine spostato `api/routes/_tool_loop.py`→`services/turn/tool_loop.py` via git mv; risolto debito god-function nel route layer), `bcc0b4a` fix Invalid-JSON via marker `MALFORMED_FRAME_KEY`. Smoke import `core.app` OK (no circular). **Suite mirata 57 test verde** (channel, tool-loop, confirmation, reflective, direct-executor×4, factory). Ruff pulito sui file toccati; mypy: solo errori pre-esistenti (es. ws.py `_sync_conversation_to_file`).
- **IMPORTANTE — WS integration test ambientali**: `test_websocket.py`/`test_concurrent`/`test_message_editing`/`test_branch_conversation`/`test_voice_*` **non girano in questo ambiente offline**: si bloccano dentro `assembler.assemble` su una chiamata reale embedding/memory senza backend ("Embedding API unreachable, fastembed fallback disabled", nessun LM Studio). **Hang IDENTICO verificato su HEAD `4bd3585` (pre-Fase-1)** → è ambientale, NON una regressione del rework. Il pattern del pump è provato sano da una repro minimale TestClient. Per girarli serve un backend embedding/LLM o mock aggiuntivi (debito di test-infra pre-esistente, fuori scope Fase 1). → la "suite completa che non finisce mai" era questo, non lentezza.
- Cleanup minori rinviati (cosmetici, non funzionali): `_receive_ws_text` in `chat/_shared.py` ora orfano (idle usa il pump); `sink._ws` escape-hatch non più usato dall'engine. Da rimuovere in un pass di pulizia.
- **PROSSIMO: Fase 2** (C+D: pipeline middleware tool + `PermissionService` centrale, spostare config conferme fuori da `pc_automation`). Vedi piano.
- (storico) Fase 1 sotto-passi:
  - ✅ **B-core FATTO** (non ancora committato): creato `backend/services/turn/channel.py` — `InteractionChannel` Protocol (`request(kind,payload,*,execution_id,timeout_s,cancel_event)->dict|None`, prop `cancelled`/`connected`), `WebSocketInteractionChannel` con UN read-pump (`start()`/`aclose()`/`begin_turn()->cancel_event`/`next_user_message()->dict|None`), `ScriptedInteractionChannel` test-double. `_REQUEST_SPECS` mappa kind→(req_type,resp_type) e include già `ask_user` (Fase 4 ready). Pump instrada: cancel→set cancel_event+resolve pending None; match execution_id+response_type→resolve future; response stale→scarta; altro→`_user_messages` queue; disconnect→`connected=False`+sentinel. `test_interaction_channel.py` (15 test) verde; ruff+mypy strict puliti. Contratto preservato vs legacy: confirm→bool via `resp and resp.get("approved")`; client_tool→cancel/timeout/disconnect distinti via prop `cancelled`/`connected`.
  - PROSSIMO PASSO (B-migrate, atomico): far partire il pump in `chat/ws.py` PRIMA del turno e spostare il main-loop idle da `receive_text()` a `channel.next_user_message()` (il pump è l'UNICO reader). Poi migrare `_tool_loop.py` `_request_confirmation`→`channel.request("tool_confirmation",...)` e `_execute_client_tool`→`channel.request("client_tool_call",...)`, e rimuovere il cancel reader `direct_executor.py:_spawn_cancel_reader` (ora è il pump). Passare `channel` lungo la catena al posto di `sink._ws`. Behavior-preserving: `test_tool_loop.py`, `test_confirmation_toggle.py`, client-tool tests. POI (A) estrarre il loop in `services/turn/tool_loop.py` e assottigliare `ws.py`/`_tool_loop.py` (shim).
  - Verifica B-core: `& .\.venv\Scripts\python.exe -m pytest tests/test_interaction_channel.py -q` da `backend/`.

Vedi [[[project-architecture]]] se esiste. Utente scrive in italiano.
